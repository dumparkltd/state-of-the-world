import { defineMessages } from 'react-intl';

export const scope = 'hrmi.containers.ChartYearSelect';

export default defineMessages({
  selectYear: {
    id: `${scope}.selectYear`,
    defaultMessage: 'Select year',
  },
});
